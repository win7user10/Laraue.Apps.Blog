## Key decisions

### Use sortOrder in sections and documentation
Sections should be always sorted in the predefined order, so the order for sections as int number is used.  
Documentation articles also use the same order always

### Articles are sorted by creation date as default
That makes it easy to see articles writing history while seeing the main blog page. UpdatedAt filter also could be added,
but it is not priority #1.